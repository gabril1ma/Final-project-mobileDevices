import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderBottomWidth: 2,
    display: "flex",
    flexDirection: "row",
  },
  searchInput: {
    borderColor: "transparent",
    width: "100%",
  },
  searchInputLabel: {
    width: "100%",
  },
});
