import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderBottomWidth: 1,
    display: "flex",
    flexDirection: "row",
    paddingBottom: 4,
  },
  searchInput: {
    borderColor: "transparent",
    width: "100%",
  },
  searchInputLabel: {
    width: "100%",
  },
});
